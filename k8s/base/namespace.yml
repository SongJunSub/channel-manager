# Namespace — 리소스를 논리적으로 분리하는 가상 클러스터
# channel-manager 프로젝트의 모든 리소스는 이 네임스페이스에 배포된다
# 다른 프로젝트의 리소스와 이름 충돌을 방지한다
apiVersion: v1
kind: Namespace
metadata:
  name: channel-manager  # 네임스페이스 이름
  labels:
    project: channel-manager  # 프로젝트 라벨
