<?xml version="1.0" encoding="UTF-8"?>
<!-- Logback Spring Boot 설정 -->
<configuration>

    <!-- 기본 프로파일: 개발 환경 (사람이 읽기 쉬운 콘솔 로그) -->
    <springProfile name="default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <!-- Phase 22: traceId, spanId를 로그에 포함 — Zipkin UI에서 Trace ID로 로그 추적 가능 -->
                <!-- %X{traceId:-}: MDC에 traceId가 있으면 출력, 없으면 빈 문자열 -->
                <pattern>%d{HH:mm:ss.SSS} %-5level [%X{requestId:---------}] [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.channelmanager" level="DEBUG"/>
    </springProfile>

    <!-- prod 프로파일: 운영 환경 (JSON 구조화 로그) -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

</configuration>
